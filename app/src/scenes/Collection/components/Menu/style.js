export const add = {
  position: 'fixed',
  bottom: 20,
  right: 20,
};

export const layout = {
  position: 'fixed',
  top: 84,
  right: 20,
};

export const layoutIcon = theme => ({
  display: 'inline-block',
  backgroundColor: theme.backgroundColor,
  marginRight: 10,
  borderRadius: '50%',
  height: 24,
  width: 24,
  padding: 8,
});
